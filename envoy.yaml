node:
  cluster: envoy_cluster
  id: cluster_id

dynamic_resources:
  lds_config:
    path: /Users/stevenzhou/envoy-test/lds.yaml # requires absolute pathing for file on machine
  cds_config:
    path: /Users/stevenzhou/envoy-test/cds.yaml # requires absolute pathing for file on machine

